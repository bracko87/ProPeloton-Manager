name: Final advisor notification localization

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/chatgpt-notifications-advisor-labels-20260828.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply advisor localization pass
        run: python .github/scripts/chatgpt_notifications_advisor_labels_20260828.py

      - name: Install dependencies
        run: npm ci

      - name: Production build
        run: npm run build

      - name: Commit product changes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/pages/dashboard/NotificationsPage.tsx \
            src/i18n/locales/en/notifications.json \
            src/i18n/locales/sr-Latn/notifications.json

          if git diff --cached --quiet; then
            echo "No product changes to commit."
            exit 0
          fi

          git commit -m "Localize advisor notification labels"
          git push origin main
